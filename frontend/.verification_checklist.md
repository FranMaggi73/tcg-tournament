# Final Verification Checklist - User Profiles & API Integration

## 1. User Profile Flow
- [ ] Create account $\rightarrow$ Go to `/profile` $\rightarrow$ Set nickname "Satoshi" $\rightarrow$ Verify Firestore `/users/{uid}` has `displayName: "Satoshi"`.
- [ ] Update nickname $\rightarrow$ Verify change reflects instantly in `/tournaments/[id]` public view.
- [ ] Verify that an account without a profile document defaults to "Jugador {uid}" or "Usuario Anónimo".

## 2. UID Resolution & Cache Efficiency
- [ ] Open a tournament with multiple players.
- [ ] Verify in Network tab that profiles are fetched in a batch (via `resolveUserProfiles`) rather than individual requests per player.
- [ ] Refresh page $\rightarrow$ Verify the cache prevents redundant requests for the same UIDs.

## 3. API Integration (Svelte $\rightarrow$ Go $\rightarrow$ Firestore)
- [ ] Match Result: Enter BO3 scores (2-1) in `MatchResultEditor` $\rightarrow$ Verify API call to `/result` $\rightarrow$ Verify match status becomes `completed` and winner is set via Firestore real-time sync.
- [ ] Round Advance: Click "Avanzar Ronda" $\rightarrow$ Verify API call to `/advance` $\rightarrow$ Verify `currentRound` increments and new pairings appear.
- [ ] Player Drop: Mark player as dropped in `ParticipantManager` $\rightarrow$ Verify API call to `/drop` $\rightarrow$ Verify status in Firestore.

## 4. Security & RBAC
- [ ] Access `/tournaments/[id]/manage` as a viewer $\rightarrow$ Verify redirection to public view.
- [ ] Verify that API requests include the `Authorization: Bearer <token>` header.
